/**
 * model_params.c — PLACEHOLDER
 * Replace this file by running the training notebook (Step 9).
 * The notebook generates the full INT8 quantized model as a C array.
 */
#include "model_params.h"

/* Placeholder — 1 byte to satisfy the linker */
const uint8_t g_model_data[MODEL_DATA_LEN] = { 0x00 };
